# HTTP Basic Auth (used by src/proxy.ts)
# On Vercel, auth is enabled automatically (VERCEL=1).
# Locally, set BASIC_AUTH_ENABLED=true to test the browser login prompt.

BASIC_AUTH_ENABLED=true
BASIC_AUTH_USER=inrcliqdemo
BASIC_AUTH_PASSWORD=inrcliqdemo@100%
